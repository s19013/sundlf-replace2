# 目的

バックエンドの入出力仕様などを書く。
実装者が「このAPIに何を送ればよくて、何を処理して、何が返るのか」を分かるようにします。

# 粒度

事細かくではなく、ざっくりとわかる程度で十分

---

# api名

laravelのルーティングの`name()`でつける名前

# 概要

このAPIは〇〇を行います。

# エンドポイント

METHOD /api/example

# 認証

必須 / 不要

# パスパラメータ

最新の詳細な型はscramble生成のOpenAPI/schema.d.ts参照

| 名前 | 型     | 必須 | 説明   |
| ---- | ------ | ---- | ------ |
| id   | string | yes  | 対象ID |

# クエリパラメータ

最新の詳細な型はscramble生成のOpenAPI/schema.d.ts参照

| 名前 | 型     | 必須 | デフォルト | 説明       |
| ---- | ------ | ---- | ---------- | ---------- |
| page | number | no   | 1          | ページ番号 |

# リクエストボディ

```json
{
  "name": "example"
}
```

# レスポンス

ステータスコード:200

```json
{
  "title": "example"
}
```

# バリデーション

バリデーションが必要なら記入

# 処理の流れ

このapiが裏でどんな処理するか

# エラー

どんなことしたらエラーになり、どんなエラーレスポンスをかえすか。

# 備考
