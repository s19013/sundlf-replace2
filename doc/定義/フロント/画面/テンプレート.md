# 画面名

ファイル名が画面名なので、ファイルには記入しなくて良い。

# 目的

この画面の機能、実行する処理、使用するapiなどをまとめ、整理する

# 粒度

事細かくではなく、ざっくりとわかる程度で十分

---

# url

この画面のurl

# 認証

必須 / 不要

# 表示項目

ボタンやフォーム類、データなどの画面上に表示するもの

例:
保存ボタン、削除ボタン、氏名記入フォーム

# 機能(ユーザーができること)

この画面が持つ機能のうち、フォームへの記入、ボタンの操作など、ユーザーがその画面上でできること

例:
保存ボタンを押して、メモを保存する
切り替えボタンを押してmdとhtmlを切り替える

# 機能(ユーザーができないこと)

ユーザーが操作できない裏側で動く機能

# 初期表示時の処理

初期表示時にすること

例:
メモ取得apiを呼び出す

# 使用API

この画面で使うapi

例:
メモ取得api

# 行動

ユーザーがどんな行動をとったらどんなことが起きるのか必要な分だけ書く

いちいち言わんくてもわかるようなことは書くだけ疲れるのでかかない。

例:検索ボタンを押したら検索を実行
-> むしろそれ以外何があるのか?
-> 言わなくてもわかるため記入は必要ない

それ以外の何か特殊な処理を一度挟むのなら記入が必要

# 備考
